# Conversion Rate

## Overview
This repository contains a Machine Learning solution to predict user conversions based on web traffic data.

## Goal
Optimize marketing strategies by predicting the `converted` target variable using behavior metrics (pages visited, age, country, user type).

## Evaluation Metric
* **F1-Score** on the minority class (`converted = 1`).